// Temporary placeholder to avoid build-time type errors in CI.
// The original `useChat` example used properties that don't match the
// current `@ai-sdk/react` type definitions. If you rely on this file for
// development, restore a valid options object matching the library's types.

export const noop = () => undefined;
